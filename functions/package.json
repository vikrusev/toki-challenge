{
  "name": "functions",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile:prod": "tsc --sourceMap false",
    "compile:debug": "tsc",
    "debug": "node --inspect node_modules/@google-cloud/functions-framework/build/src/main.js --source=./dist --target=helloData",
    "watch": "concurrently \"npm run compile:debug -- --watch\" \"nodemon --watch ./dist/ --exec npm run debug\""
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.0.1",
    "nodemon": "^2.0.22",
    "typescript": "^5.0.3"
  },
  "dependencies": {
    "@google-cloud/functions-framework": "^3.1.3",
    "@google-cloud/secret-manager": "^4.2.1",
    "@google-cloud/storage": "^6.9.5",
    "joi": "^17.9.1"
  }
}
